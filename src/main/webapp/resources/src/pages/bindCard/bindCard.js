define(function(require) {
    var baseUrl = require('../../common/js/baseUrl/baseUrl');
    return function(app) {
        app.controller('bindControl', ['$scope', '$http', function($scope, $http) {

        }])
    }
})
